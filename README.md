# Maven Scalatra archetype
Simple Scalatra skeleton using maven, Coffeescript, LESS CSS.
Running tests still doesn't work.

## Run
Start Jetty

    mvn jetty:run
    
and go to (http://localhost:8080).
